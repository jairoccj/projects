#include <bits/stdc++.h>
using namespace std;

int estourados(int numero){
    if (numero < 10){
            return numero;
        }
    
    int contador = 0;
    for (int i=1; i<=numero; i++){
    
        string num = to_string(numero);

        bool flag = true;
        for (int i=0; i < num.size(); i++){
            if (num.size() )
        }
    }
}

int main(){

}