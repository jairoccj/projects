#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, seq; cin >> n >> seq;

    vector<int> numeros(n,0);
    for (int &a : numeros) cin >> a;

    int maior = 0;

    int esquerda = 0, soma = 0;

    int valor = 0;

    for(int direita=0; direita<n; direita++){
        
    }



}